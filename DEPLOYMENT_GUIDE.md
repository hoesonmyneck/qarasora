# 🚀 Руководство по развертыванию Qarasora

## Архитектура проекта

- **Frontend**: Vue 3 + Vite (Static Site)
- **Backend**: Node.js + Express
- **Database**: PostgreSQL (Supabase)

---

## 🎯 Вариант 1: Render (Рекомендуется)

### **Преимущества:**
- ✅ Бесплатный план для старта
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификаты
- ✅ Простая настройка

### **Шаг 1: Подготовка репозитория**

1. Убедитесь, что проект в GitHub
2. Проверьте `.gitignore`:

```gitignore
# Должно быть в .gitignore
node_modules/
.env
dist/
.DS_Store
uploads/
```

3. Создайте `.env.example` для документации:

```env
# Backend
DATABASE_URL=your_supabase_connection_string
JWT_SECRET=your_secret_key_here
PORT=3001

# Frontend (будет настроено в Render)
VITE_API_URL=https://your-backend-url.onrender.com
```

### **Шаг 2: Деплой Backend на Render**

1. **Зайдите на [render.com](https://render.com)** и войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Выберите ваш репозиторий `qarasora`
   - Настройте параметры:

   ```yaml
   Name: qarasora-backend
   Region: Singapore (или ближайший)
   Branch: main
   Root Directory: backend
   Runtime: Node
   Build Command: npm install
   Start Command: npm start
   Instance Type: Free
   ```

3. **Добавьте переменные окружения** (Environment Variables):
   
   Перейдите в "Environment" и добавьте:
   
   ```
   DATABASE_URL=postgresql://postgres.ljhpzgqagwspiydxcdub:[YOUR_PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:6543/postgres
   JWT_SECRET=your_super_secret_jwt_key_here_change_this
   PORT=3001
   NODE_ENV=production
   ```

4. **Создайте папку для загрузок** (Persistent Disk):
   - В разделе "Disks" добавьте:
   - Name: `uploads`
   - Mount Path: `/opt/render/project/src/backend/uploads`
   - Size: 1GB (бесплатно)

5. **Нажмите "Create Web Service"** и дождитесь деплоя

6. **Скопируйте URL** (например: `https://qarasora-backend.onrender.com`)

### **Шаг 3: Обновите CORS в Backend**

Добавьте ваш будущий frontend URL в CORS:

```javascript
// backend/server.js
const allowedOrigins = [
  'http://localhost:5173',
  'https://qarasora.onrender.com', // Ваш frontend URL
  'https://your-custom-domain.com' // Если есть
];

app.use(cors({
  origin: function(origin, callback) {
    if (!origin || allowedOrigins.includes(origin)) {
      callback(null, true);
    } else {
      callback(new Error('Not allowed by CORS'));
    }
  },
  credentials: true
}));
```

### **Шаг 4: Деплой Frontend на Render**

1. **Создайте новый Static Site:**
   - "New +" → "Static Site"
   - Выберите тот же репозиторий `qarasora`

2. **Настройте параметры:**

   ```yaml
   Name: qarasora
   Branch: main
   Root Directory: (оставьте пустым)
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```

3. **Добавьте переменную окружения:**
   
   ```
   VITE_API_URL=https://qarasora-backend.onrender.com
   ```

4. **Нажмите "Create Static Site"**

5. **Скопируйте URL** (например: `https://qarasora.onrender.com`)

### **Шаг 5: Обновите Frontend для работы с деплоем**

Убедитесь, что `src/services/api.ts` использует переменную окружения:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3001';
```

### **Шаг 6: Настройте автодеплой**

1. В настройках обоих сервисов включите "Auto-Deploy"
2. Теперь при каждом push в GitHub проект будет автоматически обновляться

### **Важные замечания:**

⚠️ **Бесплатный план Render:**
- Backend "засыпает" после 15 минут неактивности
- Первый запрос после сна будет медленным (~30 сек)
- Решение: используйте платный план ($7/мес) или пингуйте сервер

⚠️ **Uploads:**
- На бесплатном плане файлы могут удалиться при перезапуске
- Используйте Persistent Disk или облачное хранилище (Cloudinary, S3)

---

## 🎯 Вариант 2: Vercel (Frontend) + Render (Backend)

### **Преимущества:**
- ✅ Vercel лучше для фронтенда (CDN, быстрее)
- ✅ Бесплатный план без ограничений
- ✅ Автоматические превью для PR

### **Frontend на Vercel:**

1. Зайдите на [vercel.com](https://vercel.com)
2. "Import Project" → выберите репозиторий
3. Настройки:
   ```
   Framework Preset: Vite
   Build Command: npm run build
   Output Directory: dist
   ```
4. Environment Variables:
   ```
   VITE_API_URL=https://qarasora-backend.onrender.com
   ```

### **Backend на Render:**
- Следуйте инструкциям из Варианта 1, Шаг 2

---

## 🎯 Вариант 3: Railway (Универсальный)

### **Преимущества:**
- ✅ Деплой и фронта и бэка
- ✅ Простая настройка
- ✅ $5 бесплатно каждый месяц

### **Инструкция:**

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Добавьте сервис из GitHub
4. Railway автоматически определит монорепозиторий
5. Настройте переменные окружения
6. Готово!

---

## 🎯 Вариант 4: Netlify (Frontend) + Render (Backend)

Аналогично Vercel, но используйте Netlify для frontend.

---

## 📊 Сравнение хостингов

| Платформа | Frontend | Backend | Database | Цена | Сложность |
|-----------|----------|---------|----------|------|-----------|
| **Render** | ✅ | ✅ | ❌ | $0-7/мес | ⭐⭐ |
| **Vercel + Render** | ✅✅ | ✅ | ❌ | $0 | ⭐⭐ |
| **Railway** | ✅ | ✅ | ✅ | $5-10/мес | ⭐ |
| **Netlify + Render** | ✅✅ | ✅ | ❌ | $0 | ⭐⭐ |
| **DigitalOcean** | ✅ | ✅ | ✅ | $10-20/мес | ⭐⭐⭐ |

---

## 🔧 Финальная проверка

После деплоя проверьте:

1. ✅ Backend API доступен: `https://your-backend.onrender.com/api/auth/login`
2. ✅ Frontend открывается: `https://your-frontend.onrender.com`
3. ✅ Авторизация работает
4. ✅ Загрузка файлов работает
5. ✅ База данных подключена

---

## 🆘 Частые проблемы

### **1. CORS ошибки**
- Добавьте frontend URL в `allowedOrigins` на backend

### **2. 404 на роутах Vue**
- Добавьте `_redirects` файл для Netlify/Render:
  ```
  /* /index.html 200
  ```

### **3. Backend "спит"**
- Используйте платный план или сервис пинга (cron-job.org)

### **4. Файлы не сохраняются**
- Настройте Persistent Disk или используйте S3/Cloudinary

---

## 📝 Следующие шаги

1. Настройте мониторинг (Sentry, LogRocket)
2. Добавьте аналитику (Google Analytics)
3. Настройте резервные копии БД
4. Добавьте CI/CD тесты
5. Настройте кастомный домен

---

## 🎉 Готово!

Ваш проект теперь в продакшене! 🚀

Ссылки:
- Frontend: https://your-app.onrender.com
- Backend: https://your-backend.onrender.com
- Database: Supabase (уже настроено)
