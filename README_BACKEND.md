# Qarasora - Бекенд добавлен! 🎉

## Что было сделано

Полностью реализован бекенд для сайта Qarasora с авторизацией, управлением новостями, фермами, документами и пользователями.

## 📋 Реализованный функционал

### ✅ Backend API (Node.js + Express + PostgreSQL)

1. **Авторизация и безопасность**
   - JWT токены для аутентификации
   - Хеширование паролей с bcrypt
   - Middleware для защиты маршрутов
   - Разделение прав: админ и обычный пользователь

2. **API для новостей**
   - Получение всех новостей (публичный доступ)
   - Создание новостей (только админ)
   - Обновление новостей (только админ)
   - Удаление новостей (только админ)

3. **API для ферм/хозяйств**
   - Получение списка ферм с координатами
   - Создание ферм с геолокацией
   - Обновление информации о фермах
   - Удаление ферм
   - Интеграция с картой Leaflet

4. **API для документов**
   - Загрузка файлов (PDF, DOC, XLS, изображения)
   - Хранение на сервере
   - Скачивание документов
   - Удаление документов
   - Контроль размера файлов

5. **Управление пользователями**
   - Создание новых аккаунтов (только админ)
   - Список пользователей
   - Удаление пользователей
   - Автоматическое создание тестовых аккаунтов

### ✅ Frontend интеграция

1. **API сервис (`src/services/api.ts`)**
   - Централизованное управление API запросами
   - Автоматическая отправка JWT токенов
   - Обработка ошибок
   - TypeScript типизация

2. **Обновленные страницы**
   - `PortalPage.vue` - полная интеграция с API
   - `EventsPage.vue` - загрузка новостей из базы данных
   - Динамическая загрузка данных
   - Real-time обновление карты

## 🏗️ Структура проекта

```
qarasora/
├── backend/                         # 🆕 Backend сервер
│   ├── config/
│   │   └── database.js             # Подключение к PostgreSQL
│   ├── middleware/
│   │   ├── auth.js                 # JWT аутентификация
│   │   └── upload.js               # Загрузка файлов
│   ├── routes/
│   │   ├── auth.js                 # Маршруты авторизации
│   │   ├── users.js                # Управление пользователями
│   │   ├── news.js                 # CRUD новостей
│   │   ├── farms.js                # CRUD ферм
│   │   └── documents.js            # Загрузка/скачивание файлов
│   ├── scripts/
│   │   └── initDb.js               # Инициализация БД
│   ├── uploads/                    # Загруженные файлы
│   ├── .env                        # Конфигурация
│   ├── .gitignore
│   ├── package.json
│   ├── README.md
│   └── server.js                   # Главный файл сервера
│
├── src/
│   ├── services/
│   │   └── api.ts                  # 🆕 API клиент
│   ├── pages/
│   │   ├── PortalPage.vue         # ✏️ Обновлено
│   │   └── EventsPage.vue         # ✏️ Обновлено
│   └── ...
│
├── .env                            # 🆕 Переменные окружения
├── .env.example                    # 🆕 Пример конфигурации
├── SETUP.md                        # 🆕 Руководство по установке
└── README_BACKEND.md               # 🆕 Этот файл
```

## 🚀 Быстрый старт

### 1. Установите PostgreSQL

```bash
# Windows (Chocolatey)
choco install postgresql

# macOS
brew install postgresql
brew services start postgresql

# Linux
sudo apt install postgresql
sudo systemctl start postgresql
```

### 2. Создайте базу данных

```bash
psql -U postgres
CREATE DATABASE qarasora;
\q
```

### 3. Установите зависимости

```bash
# Фронтенд
npm install

# Бекенд
cd backend
npm install
```

### 4. Инициализируйте БД

```bash
cd backend
npm run init-db
```

Будут созданы:
- ✅ Таблицы: users, news, farms, documents
- ✅ Админ: `admin` / `admin`
- ✅ Пользователь: `user` / `user`
- ✅ Тестовые данные (новости, фермы)

### 5. Запустите приложение

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```
Сервер: http://localhost:3001

**Терминал 2 - Frontend:**
```bash
npm run dev
```
Сайт: http://localhost:5173

## 📝 API Endpoints

### Авторизация
```
POST   /api/auth/login      # Вход в систему
POST   /api/auth/logout     # Выход
GET    /api/auth/me         # Текущий пользователь
```

### Пользователи (Admin only)
```
POST   /api/users           # Создать пользователя
GET    /api/users           # Список пользователей
DELETE /api/users/:id       # Удалить пользователя
```

### Новости
```
GET    /api/news            # Все новости (публичный)
GET    /api/news/:id        # Одна новость
POST   /api/news            # Создать (admin)
PUT    /api/news/:id        # Обновить (admin)
DELETE /api/news/:id        # Удалить (admin)
```

### Фермы
```
GET    /api/farms           # Все фермы (требует авторизации)
GET    /api/farms/:id       # Одна ферма
POST   /api/farms           # Создать (admin)
PUT    /api/farms/:id       # Обновить (admin)
DELETE /api/farms/:id       # Удалить (admin)
```

### Документы
```
GET    /api/documents              # Список документов
GET    /api/documents/:id          # Информация о документе
GET    /api/documents/:id/download # Скачать файл
POST   /api/documents              # Загрузить (admin)
DELETE /api/documents/:id          # Удалить (admin)
```

## 🔐 Тестовые учетные данные

После инициализации БД доступны:

| Роль | Username | Password | Права |
|------|----------|----------|-------|
| Администратор | `admin` | `admin` | Полный доступ |
| Пользователь | `user` | `user` | Просмотр данных |

## 🔧 Технологический стек

### Backend
- **Node.js** - Runtime environment
- **Express** - Web framework
- **PostgreSQL** - Реляционная база данных
- **JWT** - Авторизация
- **bcryptjs** - Хеширование паролей
- **Multer** - Загрузка файлов
- **CORS** - Кросс-доменные запросы

### Frontend
- **Vue 3** - UI Framework
- **TypeScript** - Типизация
- **Vite** - Build tool
- **Leaflet** - Карты
- **TailwindCSS** - Стилизация

## 📊 База данных

### Таблицы

**users** - Пользователи
- id, username, password (hashed), is_admin, created_at

**news** - Новости
- id, title, body, detail, date, created_by, created_at, updated_at

**farms** - Фермы/хозяйства
- id, name, region, area, lat, lng, note, created_by, created_at, updated_at

**documents** - Документы
- id, name, filename, filepath, mimetype, size, created_by, created_at

## 🎯 Использование

### Для админа

1. Войдите как `admin` / `admin`
2. Доступны вкладки:
   - **База данных** - Добавление ферм на карту
   - **Документы** - Загрузка файлов
   - **Календарь** - Создание новостей
   - **Аккаунты** - Создание пользователей

### Для пользователя

1. Войдите как `user` / `user`
2. Доступны вкладки:
   - **База данных** - Просмотр ферм на карте
   - **Документы** - Скачивание файлов
   - **Календарь** - Просмотр новостей

## 🛠️ Разработка

### Добавление новых API endpoints

1. Создайте файл в `backend/routes/`
2. Определите маршруты
3. Импортируйте в `backend/server.js`
4. Добавьте соответствующие методы в `src/services/api.ts`

### Добавление новых таблиц

1. Обновите `backend/scripts/initDb.js`
2. Запустите `npm run init-db`

## 📦 Зависимости

### Backend
```json
{
  "express": "^4.18.2",
  "cors": "^2.8.5",
  "dotenv": "^16.3.1",
  "bcryptjs": "^2.4.3",
  "jsonwebtoken": "^9.0.2",
  "pg": "^8.11.3",
  "multer": "^1.4.5-lts.1",
  "express-validator": "^7.0.1"
}
```

## 🔍 Решение проблем

Смотрите `SETUP.md` для детального руководства по решению типичных проблем.

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении вопросов обращайтесь к документации:
- `SETUP.md` - Установка и запуск
- `backend/README.md` - Backend API
- Этот файл - Общая информация

---

**Статус:** ✅ Готово к использованию

Все функции протестированы и работают. Backend полностью интегрирован с фронтендом.
