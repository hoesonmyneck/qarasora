# 🎯 Настройка Render: Два сервиса из одного репозитория

## Структура проекта

У вас **монорепозиторий** - один Git репозиторий с двумя проектами:

```
qarasora/ (корень репозитория)
├── backend/              ← Backend здесь
│   ├── package.json
│   ├── server.js
│   └── routes/
├── src/                  ← Frontend здесь
│   ├── pages/
│   └── services/
├── package.json          ← Frontend package.json
├── vite.config.ts
└── index.html
```

---

## 📦 Git: Один коммит

**Да, коммитите всё вместе:**

```bash
git add .
git commit -m "Подготовка к деплою на Render"
git push origin main
```

---

## 🚀 Render: Два сервиса

Вы создаете **2 отдельных сервиса**, оба используют **один и тот же репозиторий**, но с **разными настройками**.

---

## 1️⃣ Backend Service (Web Service)

### Создание:
1. Зайдите на [render.com](https://render.com)
2. **New +** → **Web Service**
3. Подключите GitHub (если еще не подключен)
4. **Выберите репозиторий: `qarasora`**

### Настройки:

| Параметр | Значение |
|----------|----------|
| **Name** | `qarasora-backend` |
| **Region** | `Singapore` |
| **Branch** | `main` |
| **Root Directory** | `backend` ← **ВАЖНО!** |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |
| **Instance Type** | `Free` |

### Environment Variables:

Нажмите "Advanced" → "Add Environment Variable":

```env
DATABASE_URL=postgresql://postgres.ljhpzgqagwspiydxcdub:[ВАШ_ПАРОЛЬ]@aws-1-ap-south-1.pooler.supabase.com:6543/postgres

JWT_SECRET=qarasora_secret_key_2026_production

PORT=3001

NODE_ENV=production

FRONTEND_URL=https://qarasora.onrender.com
```

⚠️ **Замените `[ВАШ_ПАРОЛЬ]`** на реальный пароль Supabase!

### Disk (опционально):

Для сохранения загруженных файлов:

- **Name**: `uploads`
- **Mount Path**: `/opt/render/project/src/backend/uploads`
- **Size**: `1 GB` (бесплатно)

### Создать:

Нажмите **"Create Web Service"** и дождитесь деплоя (~5 минут).

**Скопируйте URL**: например, `https://qarasora-backend.onrender.com`

---

## 2️⃣ Frontend Service (Static Site)

### Создание:

1. Снова **New +** → **Static Site**
2. **Выберите тот же репозиторий: `qarasora`** ← Да, тот же самый!

### Настройки:

| Параметр | Значение |
|----------|----------|
| **Name** | `qarasora` (или `qarasora-frontend`) |
| **Branch** | `main` |
| **Root Directory** | **(оставьте пустым)** ← Frontend в корне |
| **Build Command** | `npm install && npm run build` |
| **Publish Directory** | `dist` |

### Environment Variables:

```env
VITE_API_URL=https://qarasora-backend.onrender.com
```

⚠️ **Используйте URL из шага 1** (ваш backend URL)!

### Создать:

Нажмите **"Create Static Site"** и дождитесь билда (~3 минуты).

**Скопируйте URL**: например, `https://qarasora.onrender.com`

---

## 3️⃣ Обновите Backend CORS

После создания frontend, вам нужно добавить его URL в backend.

### Вариант А: Через Environment Variables (проще)

1. Откройте Backend Service в Render
2. Перейдите в **Environment**
3. Измените `FRONTEND_URL`:
   ```
   FRONTEND_URL=https://qarasora.onrender.com
   ```
4. Нажмите **"Save Changes"**
5. Backend автоматически перезапустится

### Вариант Б: Через код (уже настроено)

В `backend/server.js` уже добавлена поддержка `process.env.FRONTEND_URL`, так что ничего менять не нужно!

---

## 4️⃣ Проверка

### Backend:
- Откройте: `https://qarasora-backend.onrender.com`
- Должен вернуть JSON с информацией об API:
  ```json
  {
    "message": "Qarasora API Server",
    "endpoints": { ... }
  }
  ```

### Frontend:
- Откройте: `https://qarasora.onrender.com`
- Должен загрузиться ваш сайт
- Попробуйте авторизоваться

---

## 🎯 Как это работает?

### Один Git репозиторий → Два сервиса Render

```
GitHub: qarasora (один репозиторий)
    ↓
    ├─→ Render Service 1: Backend (Root Directory: backend/)
    │   - Читает только папку backend/
    │   - Запускает npm install в backend/
    │   - Запускает npm start
    │
    └─→ Render Service 2: Frontend (Root Directory: пусто)
        - Читает корень репозитория
        - Запускает npm install в корне
        - Собирает npm run build
        - Публикует dist/
```

### Автодеплой:

При каждом `git push`:
- ✅ Backend автоматически обновится
- ✅ Frontend автоматически пересоберется

---

## 🔄 Workflow

### Разработка:

```bash
# Локально работаете
git add .
git commit -m "Добавил новую фичу"
git push origin main
```

### Render автоматически:

1. Замечает новый коммит
2. Обновляет Backend (если изменения в backend/)
3. Обновляет Frontend (если изменения в src/, package.json и т.д.)

---

## 💡 Советы

### 1. Проверьте Root Directory
- **Backend**: обязательно укажите `backend`
- **Frontend**: оставьте пустым (корень)

### 2. Environment Variables
- Настраивайте через Render Dashboard
- Не коммитите `.env` файлы!

### 3. Логи
- Если что-то не работает, смотрите логи:
  - Backend: В Render Dashboard → Logs
  - Frontend: В Render Dashboard → Deploy Logs

### 4. Бесплатный план
- Backend "засыпает" после 15 минут
- Первый запрос будет медленным (~30 сек)
- Решение: платный план $7/мес или сервис пинга

---

## ❓ FAQ

### Q: Нужно ли создавать два репозитория?
**A:** Нет! Один репозиторий, два сервиса.

### Q: Как Render понимает, что запускать?
**A:** По параметру **Root Directory**:
- Backend: `backend` → ищет backend/package.json
- Frontend: пусто → ищет package.json в корне

### Q: Что если я изменю только frontend?
**A:** Backend не будет пересобираться (Render умный).

### Q: Можно ли использовать разные ветки?
**A:** Да, можно настроить разные ветки для разных сервисов.

### Q: Что делать, если CORS ошибка?
**A:** Убедитесь, что FRONTEND_URL в backend правильный.

---

## 🎉 Готово!

Теперь у вас:
- ✅ Один Git репозиторий
- ✅ Два сервиса на Render
- ✅ Автоматический деплой
- ✅ Бесплатный хостинг

**Ссылки:**
- Frontend: https://qarasora.onrender.com
- Backend: https://qarasora-backend.onrender.com
- Database: Supabase (уже настроена)

**Стоимость:** $0 (бесплатный план)
