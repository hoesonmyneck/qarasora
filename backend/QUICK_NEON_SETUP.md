# 🚀 Быстрая настройка Neon Database (альтернатива Supabase)

Если Supabase недоступен, Neon - отличная альтернатива!

## Преимущества Neon:
- ✅ Бесплатно 3 ГБ хранилища
- ✅ Настройка за 2 минуты
- ✅ Serverless PostgreSQL
- ✅ Отличная производительность
- ✅ Автоматическое масштабирование

---

## 📋 Пошаговая инструкция:

### 1. Регистрация

1. Откройте https://neon.tech
2. Нажмите **"Sign up"** или **"Get started"**
3. Войдите через **GitHub** (самый быстрый способ)
   - Или используйте Google/Email

### 2. Создание проекта

После входа:

1. Нажмите **"Create a project"** (или "New Project")
2. Заполните форму:
   - **Project name**: `qarasora` (или любое имя)
   - **Postgres version**: оставьте последнюю (например, 16)
   - **Region**: выберите ближайший к вам
     - Для Казахстана: `AWS Asia Pacific (Singapore)`
     - Или: `AWS Europe (Frankfurt)`
3. Нажмите **"Create project"**

### 3. Получение строки подключения

После создания проекта:

1. Вы автоматически окажетесь на странице проекта
2. Найдите блок **"Connection string"** (обычно справа вверху)
3. Убедитесь, что выбрана вкладка **"Pooled connection"**
4. Скопируйте строку подключения

Она будет выглядеть так:
```
postgresql://[user]:[password]@[hostname]/[database]?sslmode=require
```

Например:
```
postgresql://user123:AbC123xyz@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
```

### 4. Обновление backend/.env

Откройте файл `backend/.env` и замените DATABASE_URL:

```env
# Server
PORT=3001
NODE_ENV=development

# Database - Neon
DATABASE_URL=postgresql://[ВСТАВЬТЕ_ВАШУ_СТРОКУ_ПОДКЛЮЧЕНИЯ]

# JWT
JWT_SECRET=qarasora_secret_key_2025_change_in_production
JWT_EXPIRES_IN=7d

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads
```

**ВАЖНО:** Просто вставьте полную строку подключения из Neon, ничего не изменяя!

### 5. Инициализация базы данных

```bash
cd backend
npm run init-db
```

Вы должны увидеть:
```
✅ Подключено к базе данных PostgreSQL
🌐 Используется облачная база данных (SSL включён)
✅ Таблица users создана
✅ Таблица news создана
✅ Таблица farms создана
✅ Таблица documents создана
✅ База данных успешно инициализирована!
```

### 6. Запуск

```bash
# Терминал 1 - Backend
npm run dev

# Терминал 2 - Frontend (в корневой папке)
npm run dev
```

---

## 🎯 Готово!

Теперь ваше приложение использует Neon вместо Supabase.

---

## 📊 Просмотр данных в Neon

1. Откройте https://console.neon.tech
2. Выберите ваш проект
3. Перейдите в **"Tables"** (в левом меню)
4. Здесь вы можете видеть все таблицы и данные

Или используйте **SQL Editor**:
1. Нажмите **"SQL Editor"** в левом меню
2. Можете выполнять SQL запросы:
   ```sql
   SELECT * FROM users;
   SELECT * FROM news;
   SELECT * FROM farms;
   ```

---

## 💡 Советы по Neon

### Автоматическая приостановка
Neon автоматически приостанавливает БД при неактивности (экономия ресурсов).
Первый запрос после приостановки может занять 2-3 секунды (cold start).

### Ветвление (Branching)
Neon поддерживает ветвление БД как в Git!
Можете создать копию БД для тестирования:
1. В консоли Neon нажмите **"Branches"**
2. Создайте новую ветку
3. Используйте отдельную строку подключения для тестов

### Мониторинг
В консоли Neon доступна статистика:
- Количество запросов
- Использование хранилища
- Активность соединений

---

## 🔄 Переключение обратно на Supabase

Если позже захотите вернуться к Supabase:

1. Откройте `backend/.env`
2. Замените DATABASE_URL на строку от Supabase
3. Запустите `npm run init-db` снова

Все данные останутся в Neon, но создастся новая БД в Supabase.

---

## 🆘 Проблемы с Neon?

### "connection timeout"
- Проверьте интернет-соединение
- Убедитесь, что скопировали полную строку подключения

### "password authentication failed"
- Скопируйте строку подключения заново из Neon
- Убедитесь, что нет лишних пробелов

### "database does not exist"
- Используйте **Pooled connection**, а не Direct connection

---

## 📞 Нужна помощь?

1. Проверьте консоль Neon: https://console.neon.tech
2. Документация: https://neon.tech/docs
3. Попробуйте тестовое подключение:
   ```bash
   cd backend
   node test-connection.js
   ```

---

**Преимущество Neon:** Он обычно быстрее и стабильнее для начинающих проектов!
