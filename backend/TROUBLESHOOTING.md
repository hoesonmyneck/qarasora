# 🔧 Решение проблем с подключением к базе данных

## Ошибка: ENOTFOUND db.ljhpzgqagwspiydxcdub.supabase.co

Эта ошибка означает, что система не может найти сервер базы данных.

### Решение 1: Проверьте интернет-соединение

```bash
# Windows PowerShell
Test-Connection -ComputerName db.ljhpzgqagwspiydxcdub.supabase.co -Count 2

# Или попробуйте в браузере открыть
# https://db.ljhpzgqagwspiydxcdub.supabase.co
```

### Решение 2: Проверьте файрвол/антивирус

1. Откройте **Панель управления** → **Брандмауэр Windows**
2. Нажмите **"Разрешить приложение или компонент"**
3. Найдите **Node.js** и убедитесь, что разрешено для частных и общедоступных сетей
4. Если Node.js нет в списке, нажмите **"Разрешить другое приложение"** и добавьте

### Решение 3: Проверьте DNS

```powershell
# Очистите кэш DNS
ipconfig /flushdns

# Попробуйте использовать Google DNS
# 1. Панель управления → Сеть и Интернет → Центр управления сетями
# 2. Ваше подключение → Свойства → IPv4 → Свойства
# 3. Установите DNS: 8.8.8.8 и 8.8.4.4
```

### Решение 4: VPN/Proxy

Если вы используете VPN или прокси:
1. Попробуйте отключить VPN
2. Или настройте исключения для Supabase

### Решение 5: Проверьте правильность данных в Supabase

1. Откройте https://supabase.com
2. Войдите в свой проект
3. Settings → Database
4. Проверьте **Connection string** и **Host**
5. Убедитесь, что проект активен (не приостановлен)

---

## Ошибка: password authentication failed

### Решение: Проверьте пароль

В строке подключения DATABASE_URL специальные символы должны быть закодированы:

| Символ | Код |
|--------|-----|
| `$` | `%24` |
| `@` | `%40` |
| `#` | `%23` |
| `%` | `%25` |
| `&` | `%26` |
| `=` | `%3D` |

**Пример:**
```env
# Пароль: MyPass$123
DATABASE_URL=postgresql://postgres:MyPass%24123@db.xxx.supabase.co:5432/postgres
```

---

## Ошибка: timeout

### Решение: Увеличьте таймаут

Уже сделано в `config/database.js`:
```javascript
connectionTimeoutMillis: 20000 // 20 секунд
```

Если всё равно таймаут, проверьте скорость интернета.

---

## Ошибка: SSL connection error

### Решение: Проверьте настройки SSL

В `config/database.js` должно быть:
```javascript
ssl: {
  rejectUnauthorized: false
}
```

---

## ✅ Тест подключения

Создайте файл `backend/test-connection.js`:

```javascript
import pg from 'pg';
import dotenv from 'dotenv';

dotenv.config();

const { Client } = pg;

async function testConnection() {
  console.log('🔍 Тестирование подключения к базе данных...\n');
  
  const client = new Client({
    connectionString: process.env.DATABASE_URL,
    ssl: {
      rejectUnauthorized: false
    }
  });

  try {
    console.log('📡 Подключение к:', process.env.DATABASE_URL?.replace(/:[^:]*@/, ':****@'));
    await client.connect();
    console.log('✅ Успешное подключение!');
    
    const res = await client.query('SELECT NOW()');
    console.log('⏰ Время сервера:', res.rows[0].now);
    
    await client.end();
    console.log('✅ Тест завершён успешно!');
  } catch (err) {
    console.error('❌ Ошибка подключения:', err.message);
    console.error('\nПодробности:', err);
    process.exit(1);
  }
}

testConnection();
```

Запустите:
```bash
node test-connection.js
```

---

## 🆘 Если ничего не помогло

### Вариант A: Используйте альтернативную облачную БД

1. **Neon** (https://neon.tech)
   - Бесплатно 3 ГБ
   - Быстрая регистрация
   - Отличная производительность

2. **Railway** (https://railway.app)
   - $5 бесплатных кредитов
   - Простая настройка

### Вариант B: Локальная PostgreSQL (временно)

Если нужно быстро протестировать:

1. Скачайте портативную версию PostgreSQL
2. Или используйте Docker:
   ```bash
   docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres
   ```

Затем в `.env`:
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
```

---

## 📞 Дополнительная помощь

Если проблема не решается, соберите информацию:

1. Версия Node.js: `node --version`
2. Операционная система
3. Текст ошибки полностью
4. Результат теста подключения
5. Можете ли открыть Supabase в браузере

И обратитесь за помощью с этими данными.
